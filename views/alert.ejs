<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        html{
            height: 100%;
            margin: 0px;
            overflow: hidden;
        }
        body{
            background-image: url("https://cache.careers360.mobi/media/article_images/2022/5/25/exam-results-featured-image.jpg");
            height: 792px;
            margin: 0px;
            position: relative;
            top: -17px;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .con1{
            background-color: rgb(161, 161, 205);
            height: 100%;
            width: 100%;
            position: absolute;
        }.con2{
            position: relative;
            bottom:30px;
            z-index: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            border-radius: 5px;
            background-color: rgba(255, 255, 255, 0.706);
            padding:80px 200px;
            font-size: 20px;
            box-shadow:0px 0px 30px salmon;
        }.con2 button{
            font-size: 12px;
            padding:8px 40px;
            background-color:rgb(240, 154, 154);
            cursor: pointer;
            border-radius: 10px;border:1px solid azure;
        }
        .con2 button:hover{
            background-color:greenyellow;
        }
    </style>
</head>
<body>
    <div class="con1">
    </div>
    <div class="con2">
        <p><%= message %> </p>
        <%   if(choice==1){ %>
            <form action="/student" method="get">
                <button>OK</button>
            </form>
        <%  }else if(choice==2){  %>
            <form action="/admin" method="get">
                <button name="btn">OK</button>
            </form>
        <%  }else{  %>
            <form action="/admin" method="post">
                <button name="btn">OK</button>
            </form>
        <%  }  %>
    </div>
</body>
</html>